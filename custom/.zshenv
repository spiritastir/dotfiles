# Path to zsh dotfiles
export ZDOTDIR="$HOME/.config/zsh"

# Path to Zim
export ZIM_HOME="${ZDOTDIR:-${HOME}}/.zim"

# Path to starship configuration file
export STARSHIP_CONFIG="${ZDOTDIR:-${HOME}}/starship.toml"
